# ── docsearch-ingestion — dépendances Python ─────────────────
# Indexation, workers Kafka, surveillance de dossier

# Elasticsearch 9.x (requiert Python >= 3.10)
elasticsearch==9.4.1

# Client Tika (appelle les serveurs Tika distants via HTTP —
# aucun JRE local nécessaire dans ce conteneur)
tika==3.1.0

# File de messages
# kafka-python 2.0.2 casse sous Python 3.12 (ModuleNotFoundError:
# kafka.vendor.six.moves) — kafka-python-ng est le fork maintenu.
kafka-python-ng==2.2.3

# Surveillance dossier (PollingObserver requis pour CIFS/NFS)
watchdog==4.0.0

# Support des archives .7z (zip, tar.* utilisent la bibliothèque
# standard Python — aucune dépendance nécessaire pour ces formats)
py7zr==0.21.1

# Configuration dynamique (extensions et tailles max autorisées),
# modifiable à chaud sans redémarrer les conteneurs
redis==5.2.0

# ── Connecteur SQL (sql_indexer.py / sql_worker.py) ───────────
# SQLAlchemy : couche générique au-dessus de PostgreSQL/MySQL — le choix
# du type de base se résume au préfixe du DSN (postgresql+psycopg2://
# vs mysql+pymysql://), une seule logique d'extraction pour les deux.
SQLAlchemy==2.0.36
# psycopg2-binary : wheel précompilée, pas de dépendances système à
# installer dans l'image (contrairement à psycopg2 "source").
psycopg2-binary==2.9.9
# PyMySQL : driver MySQL pur Python, pas de dépendances de compilation.
PyMySQL==1.1.1
# Déchiffrement des DSN dynamiques enregistrés via le panneau d'administration
# (sql_dsn_registry.py) — même clé DSN_ENCRYPTION_KEY que docsearch-api
cryptography==49.0.0
